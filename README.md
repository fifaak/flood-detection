# FloodGuard: ระบบคาดการณ์ระดับน้ำท่วม

เว็บแอปพลิเคชันสำหรับคาดการณ์ระดับน้ำและโอกาสการเกิดน้ำท่วม โดยใช้สมการ Saint-Venant และวิธีของออยเลอร์

## 🏗️ โครงสร้างโปรเจค

```
flood-detection/
├── src/                          # Source code หลัก
│   ├── models/                   # โมเดลทางคณิตศาสตร์
│   │   ├── __init__.py
│   │   └── saint_venant.py       # Saint-Venant solver
│   ├── ui/                       # User Interface components
│   │   ├── __init__.py
│   │   └── components.py         # Streamlit UI components
│   └── utils/                    # Utilities
│       ├── __init__.py
│       ├── config.py             # Configuration settings
│       └── validators.py         # Parameter validation
├── tests/                        # Unit tests
│   ├── __init__.py
│   └── test_saint_venant.py      # Tests for solver
├── reference/                    # Reference documents
├── main.py                       # Main application entry point
├── requirements.txt              # Python dependencies
├── setup.py                      # Package setup
├── .gitignore                    # Git ignore rules
└── README.md                     # This file
```

## 🚀 การติดตั้งและรัน

### วิธีที่ 1: ใช้ Virtual Environment (แนะนำ)

1. **สร้าง Virtual Environment:**
```bash
python -m venv flood_prediction_env
```

2. **เปิดใช้งาน Virtual Environment:**
```bash
# Windows
flood_prediction_env\Scripts\activate

# macOS/Linux
source flood_prediction_env/bin/activate
```

3. **ติดตั้ง Dependencies:**
```bash
pip install -r requirements.txt
```

4. **รันแอปพลิเคชัน:**
```bash
streamlit run main.py
```

### วิธีที่ 2: ใช้ Scripts ที่เตรียมไว้

**Windows:**
```bash
# Batch file
run_app.bat

# PowerShell
.\run_app.ps1
```

## 🧪 การทดสอบ

รัน unit tests:
```bash
python -m pytest tests/ -v
```

หรือรันแบบ unittest:
```bash
python -m unittest tests.test_saint_venant -v
```

## 🎯 คุณสมบัติ

- 🌊 **คำนวณระดับน้ำ**: ใช้สมการ Saint-Venant กับวิธีของออยเลอร์
- 📊 **แสดงผลครบถ้วน**: ตาราง, กราฟ, และเมตริกส์สำคัญ
- 🎯 **คาดการณ์น้ำท่วม**: วิเคราะห์ความเสี่ยงอัตโนมัติ
- 🔧 **ปรับแต่งได้ง่าย**: Interface ที่ใช้งานง่าย
- ✅ **ตรวจสอบข้อมูล**: Validation และ warning system
- 🇹🇭 **ภาษาไทย**: รองรับภาษาไทยเต็มรูปแบบ
- 🧪 **ทดสอบได้**: Unit tests ครอบคลุม

## 📐 หลักการทางคณิตศาสตร์

### สมการ Saint-Venant (Simplified gradually varied unsteady flow)
- ใช้สำหรับการไหลของน้ำในลำธารที่เปลี่ยนแปลงตามเวลา
- เป็นสมการพื้นฐานในอุทกวิทยาสำหรับการคาดการณ์ระดับน้ำ

### วิธีของออยเลอร์ (Euler's Method)
- วิธีการเชิงตัวเลขสำหรับแก้สมการเชิงอนุพันธ์
- สูตร: `yi = yi-1 + (dy/dx) × Δx`

### การคำนวณ
1. **ช่วงระยะทาง**: `Δx = L/n`
2. **ความลึกน้ำ**: `yi = yi-1 + (dy/dx) × Δx`
3. **ระดับน้ำ**: `Wi = yi + z`
4. **การตัดสินใจน้ำท่วม**: เมื่อ `Wi > z`

## 🔧 การพัฒนา

### Clean Code Principles
- **Separation of Concerns**: แยกโมเดล, UI, และ utilities
- **Single Responsibility**: แต่ละ class มีหน้าที่เดียว
- **Dependency Injection**: ใช้ configuration และ validation
- **Type Hints**: ระบุ type ทุกฟังก์ชัน
- **Documentation**: มี docstring ครบถ้วน

### Architecture
- **Models**: `saint_venant.py` - ตรรกะการคำนวณ
- **UI**: `components.py` - Streamlit components
- **Utils**: `config.py`, `validators.py` - สนับสนุน
- **Tests**: Unit tests สำหรับ core logic

## 📝 การใช้งาน

1. **เปิดแอปพลิเคชัน** ตาม instructions ข้างต้น
2. **ตั้งค่าพารามิเตอร์** ใน sidebar:
   - ความลึกเริ่มต้นของน้ำ (y₀)
   - ระยะทางรวมของลำน้ำ (L)
   - จำนวนช่วงคำนวณ (n)
   - ค่าความขรุขระ (Manning's n)
   - ระดับตลิ่ง (z)
   - ค่า dy/dx และ S₀
3. **กดปุ่มคำนวณ** เพื่อดูผลลัพธ์
4. **วิเคราะห์ผล** จากตาราง, กราฟ, และข้อสรุป

## 🤝 การมีส่วนร่วม

1. Fork repository
2. สร้าง feature branch
3. เขียน tests สำหรับ features ใหม่
4. ตรวจสอบว่า tests ผ่านทั้งหมด
5. สร้าง Pull Request

## 📄 License

MIT License - ดูรายละเอียดใน LICENSE file

## 👥 Credits

พัฒนาโดยใช้:
- **Streamlit** - Web framework
- **NumPy** - Numerical computing
- **Pandas** - Data manipulation
- **Plotly** - Interactive visualization