# 🚀 การ Deploy แอปพยากรณ์น้ำท่วมบน Vercel

## ขั้นตอนการ Deploy

### 1. เตรียมโปรเจกต์
```bash
# ติดตั้ง dependencies
npm install

# ทดสอบรันบน web ก่อน
npm run web
```

### 2. Deploy ผ่าน Vercel CLI (แนะนำ)

```bash
# ติดตั้ง Vercel CLI
npm install -g vercel

# Login เข้า Vercel
vercel login

# Deploy
vercel
```

### 3. Deploy ผ่าน Vercel Dashboard

1. ไปที่ https://vercel.com
2. เข้าสู่ระบบด้วย GitHub/GitLab/Bitbucket
3. คลิก "Add New Project"
4. เลือก repository ของโปรเจกต์นี้
5. Vercel จะตรวจจับ config อัตโนมัติ
6. คลิก "Deploy"

## ⚙️ Configuration Files

- `vercel.json` - การตั้งค่า build และ routing
- `.vercelignore` - ไฟล์ที่ไม่ต้อง upload

## 🔧 Environment Variables (ถ้ามี)

ถ้าโปรเจกต์ต้องการ environment variables:
1. ไปที่ Project Settings > Environment Variables
2. เพิ่มตัวแปรที่จำเป็น
3. Redeploy โปรเจกต์

## 📱 หมายเหตุ

- แอปนี้จะรันเป็น **Web App** บน Vercel
- สำหรับ iOS/Android ให้ใช้ Expo Go หรือ build ผ่าน EAS
- การแสดงผลบน web อาจแตกต่างจากมือถือเล็กน้อย

## 🐛 Troubleshooting

### Build ล้มเหลว
```bash
# ลองรันคำสั่ง build ใน local ก่อน
npm run build

# ตรวจสอบ error logs
```

### Font ไม่โหลด
- ตรวจสอบว่า `expo-font` ทำงานถูกต้อง
- อาจต้องเพิ่ม fallback fonts สำหรับ web

### Routing ไม่ทำงาน
- ตรวจสอบ `vercel.json` ว่ามี rewrites ถูกต้อง
- ใช้ `react-navigation` สำหรับ web routing
